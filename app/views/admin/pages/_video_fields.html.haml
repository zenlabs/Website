.nested-fields
  = f.inputs do
    = f.input :url, :label => 'Video'
    - if !f.object.new_record? and videotron = get_video(f.object.url)
      = image_tag videotron.thumbnail
    = link_to_remove_association "Remove video", f
