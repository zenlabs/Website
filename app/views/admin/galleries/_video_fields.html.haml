.nested-fields
  = f.inputs do
    = f.input :url
    - if !f.object.new_record? && videotron = get_video(f.object.url)
      = image_tag videotron.thumbnail
    = link_to_remove_association "remove video", f
